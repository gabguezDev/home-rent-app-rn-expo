import React from "react";
import { View, Text } from "react-native";

type Props = {};

export const NearbyScreen = (props: Props) => {
	return (
		<View>
			<Text>NearbyScreen</Text>
		</View>
	);
};
